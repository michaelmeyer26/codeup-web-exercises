function showMultiplicationTable(value) {
    for (var x = 1; x <= 10; x++) {
        var multiplied = value * x;
        console.log(value + " x " + x + " = " + multiplied);
    }
}

var random = Math.floor((Math.random() * 200) + 20);

function randomEvenOrOdd(value) {

}